{% import 'macros/macros.html.twig' as macros %}

<ul {{ tree ? 'class="tree"' : '' }} role="menu" aria-label="{{ 'THEME_QUARK_A11Y.ARIA_LABELS.MAIN_NAVIGATION'|t }}">
    {{ macros.nav_loop(pages) }}
</ul>
